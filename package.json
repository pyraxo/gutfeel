{
  "name": "gutfeel-web",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:parity": "tools/test-parity.sh",
    "cloudflare:check": "wrangler deploy --dry-run --outdir tmp/cloudflare-dry-run",
    "cloudflare:deploy": "wrangler deploy --containers-rollout=immediate"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7",
    "wrangler": "^4.133.0"
  }
}
